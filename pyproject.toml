[project]
name = "corrections-chicagocrashes"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pyyaml"]

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
